apiVersion: v1
kind: Service
metadata:
  name: {{ include "LTIWEBAPPDEMO_Chart.fullname" . }}
  labels:
    {{- include "LTIWEBAPPDEMO_Chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "LTIWEBAPPDEMO_Chart.selectorLabels" . | nindent 4 }}
